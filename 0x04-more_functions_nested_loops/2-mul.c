#include <stdio.h>

/**
 * mul - A function that multiplies two integers.
 * @a: first number
 * @b: second number
 *
 * Returns: the product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
